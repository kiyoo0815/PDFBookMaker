# PDF Book Maker 사용자 매뉴얼

PDF Book Maker를 이용하여 여러 PDF 파일을 하나의 전자책으로 만드는 방법을 설명합니다.

---

# 1. PDF Book Maker 소개

PDF Book Maker는 여러 PDF 파일을 원하는 순서로 병합하고, 표지·목차·북마크·페이지 번호를 추가하여 하나의 전자책 PDF로 만드는 프로그램입니다.

주요 기능은 다음과 같습니다.

- 여러 PDF 파일 선택 및 병합
- PDF 순서 변경
- 전자책에 표시할 제목 수정
- 표지 생성
- 목차 생성
- PDF 북마크 생성
- 연속 페이지 번호 생성
- 표지 및 페이지 번호 미리보기
- 설정 자동 저장 및 복원

---

# 2. 메인 화면

![PDF Book Maker 메인 화면](docs/images/01_main.png)

메인 화면에서는 전자책에 포함할 PDF 파일을 선택하고 순서와 제목을 정한 후 출력 위치를 지정합니다.

## 2.1 PDF 파일 선택

1. `파일 선택` 버튼을 누릅니다.
2. 전자책으로 만들 PDF 파일을 선택합니다.
3. 여러 파일을 한 번에 선택할 수 있습니다.
4. 선택한 파일이 왼쪽 목록에 표시됩니다.

> PDF 파일을 선택하지 않고 전자책 만들기를 실행하면 안내 메시지가 표시됩니다.

---

# 3. PDF 순서 변경

전자책에 들어갈 PDF의 순서는 왼쪽 목록에서 확인할 수 있습니다.

순서를 변경하려면:

1. 이동할 PDF를 선택합니다.
2. `▲ 위로` 또는 `▼ 아래로` 버튼을 누릅니다.
3. 원하는 순서가 될 때까지 반복합니다.

화면에 표시된 순서대로 최종 PDF가 병합됩니다.

---

# 4. 북마크 제목 수정

각 PDF의 원본 파일명과 별도로 목차와 PDF 북마크에 사용할 제목을 지정할 수 있습니다.

1. 목록에서 수정할 PDF를 선택합니다.
2. 오른쪽의 `선택한 PDF 북마크` 입력란에 원하는 제목을 입력합니다.
3. `적용` 버튼을 누릅니다.

수정한 제목은 PDF 목록의 `북마크 제목`에 반영되며, 생성된 전자책의 목차와 PDF 북마크에 사용됩니다.

---

# 5. 출력 위치 설정

## 5.1 출력 폴더

`찾아보기` 버튼을 눌러 완성된 PDF를 저장할 폴더를 선택합니다.

## 5.2 최종 PDF 파일명

`최종 PDF 파일명`에 생성할 전자책의 파일명을 입력합니다.

예:

```text
나의전자책
```

출력 폴더 또는 파일명을 지정하지 않으면 안내 메시지가 표시됩니다.

이미 같은 이름의 파일이 존재하는 경우에는 덮어쓰기 여부를 확인합니다.

---

# 6. 전자책 설정

메인 화면의 `전자책 설정...` 버튼을 누르면 표지와 페이지 번호 등을 설정할 수 있습니다.

---

# 7. 표지 설정

![표지 설정 화면](docs/images/02_cover_settings.png)

표지 설정에서는 제목, 부제목, 하단 정보와 이미지를 구성할 수 있습니다.

## 7.1 제목

표지에 표시할 제목을 입력할 수 있습니다.

설정 가능한 항목:

- 내용
- 글자 크기
- 정렬
- 세로 위치

미리보기의 제목을 마우스로 위아래로 드래그하여 위치를 조절할 수도 있습니다.

---

## 7.2 부제목

제목 아래에 표시할 부제목을 설정합니다.

설정 가능한 항목:

- 내용
- 글자 크기
- 정렬
- 세로 위치

부제목도 미리보기에서 위아래로 드래그하여 위치를 조절할 수 있습니다.

---

## 7.3 하단 정보

표지 아래쪽에 대학명, 담당교수 등 필요한 정보를 여러 줄로 표시할 수 있습니다.

### 정보 입력

표지 정보 표의 각 행에 다음 내용을 입력합니다.

- `항목` : 정보의 이름
- `내용` : 실제 표시할 내용

예:

- 항목 : `대학명` / 내용 : `부산디지털대학교`
- 항목 : `담당교수` / 내용 : `○○○ 교수`

### 행 추가 및 삭제

- `추가` 버튼을 누르면 새로운 행을 추가할 수 있습니다.
- 삭제할 행을 선택하고 `삭제` 버튼을 누르면 해당 행이 삭제됩니다.

### 표시 설정

하단 정보 전체에 다음 설정을 적용할 수 있습니다.

- 글자 크기
- 정렬
- 세로 위치
- 줄 간격

`세로 위치`는 하단 정보 전체의 위치를 조절하고, `줄 간격`은 각 정보 사이의 간격을 조절합니다.

하단 정보는 오른쪽 미리보기에서 마우스로 위아래로 드래그하여 세로 위치를 조절할 수도 있습니다.

### 빈 행 사용

항목과 내용을 모두 비워 둔 행은 빈 줄로 처리됩니다.

따라서 정보 사이에 여백을 넣고 싶을 때 빈 행을 사용할 수 있습니다. 빈 행에는 콜론(`:`)이 표시되지 않습니다.

---

## 7.4 표지 그림

표지에 그림을 추가할 수 있습니다.

### 그림 선택

1. `그림 선택` 버튼을 누릅니다.
2. 표지에 사용할 그림 파일을 선택합니다.
3. 선택한 그림의 경로가 `그림` 입력란에 표시됩니다.
4. 오른쪽 미리보기에서 선택한 그림을 확인합니다.

### 그림 설정

설정 가능한 항목:

- 크기(%)
- 정렬
- 세로 위치

`크기(%)`를 이용하여 그림의 크기를 조절할 수 있습니다.

`정렬`에서는 그림을 왼쪽, 가운데 또는 오른쪽에 배치할 수 있습니다.

`세로 위치`를 이용하여 그림의 위아래 위치를 조절할 수 있습니다.

그림은 오른쪽 미리보기에서 마우스로 위아래로 드래그하여 세로 위치를 조절할 수도 있습니다.

---

# 8. 페이지 번호 설정

![페이지 번호 설정 화면](docs/images/03_page_number_settings.png)

페이지 번호 메뉴에서는 전자책 본문에 표시할 페이지 번호를 설정합니다.

## 8.1 짝수 / 홀수 페이지 위치

짝수 페이지와 홀수 페이지의 번호 위치를 각각 지정할 수 있습니다.

각 페이지 그림에 표시된 위치 선택 버튼을 눌러 다음 위치 중 하나를 선택합니다.

- 위쪽 왼쪽
- 위쪽 가운데
- 위쪽 오른쪽
- 아래쪽 왼쪽
- 아래쪽 가운데
- 아래쪽 오른쪽

`짝수/홀수 동일하게 적용`을 선택하면 짝수 페이지와 홀수 페이지에 같은 위치 설정을 적용할 수 있습니다.

## 8.2 크기

페이지 번호의 글자 크기를 지정합니다.

## 8.3 시작 번호

본문의 첫 페이지에 표시할 시작 번호를 지정합니다.

예를 들어 시작 번호를 `3`으로 설정하면 본문의 첫 페이지부터 3, 4, 5... 순서로 페이지 번호가 표시됩니다.

## 8.4 가로 / 세로 여백

페이지 번호와 페이지 가장자리 사이의 간격을 조절합니다.

- `가로 여백` : 페이지의 왼쪽 또는 오른쪽 가장자리와 페이지 번호 사이의 간격
- `세로 여백` : 페이지의 위쪽 또는 아래쪽 가장자리와 페이지 번호 사이의 간격

단위는 mm입니다.

## 8.5 하이픈(-) 사용

`하이픈(-) 사용`을 선택하면 페이지 번호 앞뒤에 하이픈이 표시됩니다.

예:

```text
-3-
```

선택하지 않으면 숫자만 표시됩니다.

```text
3
```

## 8.6 페이지 번호 미리보기

오른쪽 미리보기에는 병합 대상으로 선택한 PDF의 첫 페이지가 표시됩니다.

현재 설정한 페이지 번호의 위치, 크기 및 표시 형태를 실제 PDF를 생성하기 전에 확인할 수 있습니다.

페이지 번호 위치는 마우스로 드래그하지 않고 위치 선택과 가로·세로 여백을 이용하여 조절합니다.

---

# 9. 전자책 만들기

모든 설정이 끝났으면 메인 화면으로 돌아옵니다.

전자책을 만들기 전에 다음 항목을 확인합니다.

- PDF 파일과 병합 순서
- 목차 제목
- 출력 폴더
- 출력 파일명
- 표지 설정
- 페이지 번호 설정

확인이 끝났으면 화면 아래의 `전자책 만들기` 버튼을 누릅니다.

전자책을 생성하는 동안 `진행상황`과 `로그`에서 작업 상태를 확인할 수 있습니다.

작업이 정상적으로 끝나면 다음과 같은 완료 메시지가 표시됩니다.

> 전자책이 생성되었습니다.

`OK` 버튼을 누른 후 지정한 출력 폴더에서 생성된 PDF 파일을 확인합니다.

---

# 10. 완성된 전자책

![전자책 생성 결과](docs/images/04_result.png)

생성된 PDF에는 설정에 따라 다음 항목이 포함됩니다.

- 표지
- 자동 목차
- 병합된 PDF 본문
- 연속 페이지 번호
- PDF 북마크

목차와 북마크에는 메인 화면에서 지정한 전자책 제목이 사용됩니다.

---

# 11. 설정 자동 저장

표지와 페이지 번호 등의 설정값은 자동으로 저장됩니다.

프로그램을 종료한 후 다시 실행해도 이전에 사용한 주요 설정값이 복원됩니다.

따라서 매번 같은 설정을 다시 입력할 필요가 없습니다.

---

# 12. 오류 및 안내 메시지

## PDF 파일을 선택하지 않은 경우

먼저 병합할 PDF 파일을 선택해야 합니다.

## 출력 폴더를 선택하지 않은 경우

완성된 PDF를 저장할 폴더를 지정해야 합니다.

## 파일명을 입력하지 않은 경우

최종 PDF 파일명을 입력해야 합니다.

## 같은 이름의 PDF가 이미 있는 경우

기존 파일을 덮어쓸 것인지 확인하는 메시지가 표시됩니다.

기존 파일을 유지하려면 `No`를 선택합니다.

---

# 13. 현재 제한사항

현재 버전에서는 다음 사항을 지원하지 않습니다.

- 암호가 설정된 PDF
- ZIP 파일 직접 병합

대용량 또는 페이지 수가 많은 PDF를 병합하는 동안 프로그램 화면이 일시적으로 응답하지 않는 것처럼 보일 수 있습니다.

표지 제목의 글자 크기를 매우 크게 설정하면 미리보기에서 글자가 일부 잘려 보일 수 있습니다. 실제 생성된 PDF에서는 정상적으로 출력됩니다.

페이지 번호 미리보기는 실제 출력 위치를 확인하기 위한 기능이며, 미리보기 화면의 크기와 실제 PDF의 표시 크기는 다르게 보일 수 있습니다.

---

# 14. 사용 권장 순서

처음 사용하는 경우 다음 순서로 작업하는 것을 권장합니다.

1. PDF 파일 선택
2. PDF 순서 정리
3. 북마크 제목 수정
4. 출력 폴더 지정
5. 최종 PDF 파일명 입력
6. 표지 설정
7. 페이지 번호 설정
8. 미리보기 확인
9. 전자책 만들기
10. 완성된 PDF의 목차와 북마크 확인

---

PDF Book Maker  
v1.0 RC